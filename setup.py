from setuptools import setup, find_packages

setup(
    name="Online_projects",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "requests",  # Зависимости проекта
    ],
    author="Вологдина В.В.",
)
